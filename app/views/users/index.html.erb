

<%= form_with authenticity_token: :false, model: @session, url: 'sessions#create', action: '/create' do |f| %>

    <%= f.label :name %>
    <%= f.text_field :name %>

    <%= f.label :password %>
    <%= f.password_field :password %>

    <%= f.submit "Log In" %>

    <% end %>
    